import LoginCard from "./LoginCard";

const Login = () => {
    return (
        <div className="container max-w-screen-lg mx-auto flex justify-center items-center h-screen">
            <LoginCard />
        </div>
    )
}

export default Login;
